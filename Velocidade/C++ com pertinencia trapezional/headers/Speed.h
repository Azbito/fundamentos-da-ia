#pragma once
class Speed
{
public:
    double trapmf(double x, double a, double b, double c, double d);
    double lowSpeed(double speed);
    double moderateSpeed(double speed);
    double highSpeed(double speed);
    double lowAcceleration(double acceleration);
    double moderateAcceleration(double acceleration);
    double highAcceleration(double acceleration);
};

